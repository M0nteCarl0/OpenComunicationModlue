language: minimal
dist: xenial

addons:
  apt:
    sources:
      - sourceline: ppa:team-gcc-arm-embedded/ppa
    packages:
      - gcc-arm-embedded

install: |
  # Standard Peripheral Library
  StdPeriph_LIB_FILE="en.stm32f4_dsp_stdperiph_lib.zip"
  wget -O "$StdPeriph_LIB_FILE" "https://www.st.com/content/ccc/resource/technical/software/firmware/e2/a0/0f/e7/bd/4f/4b/da/stm32f4_dsp_stdperiph_lib.zip/files/stm32f4_dsp_stdperiph_lib.zip/jcr:content/translations/en.stm32f4_dsp_stdperiph_lib.zip" &&
  unzip "$StdPeriph_LIB_FILE" -d libs &&
  rm "$StdPeriph_LIB_FILE"
  
  # USB Library
  USB_LIB_FILE="en.stm32_f105-07_f2_f4_usb-host-device_lib.zip"
  wget -O "$USB_LIB_FILE" "https://my.st.com/content/ccc/resource/technical/software/firmware/44/b1/92/be/d3/26/4e/df/stm32_f105-07_f2_f4_usb-host-device_lib.zip/files/stm32_f105-07_f2_f4_usb-host-device_lib.zip/jcr:content/translations/en.stm32_f105-07_f2_f4_usb-host-device_lib.zip" &&
  unzip "$USB_LIB_FILE" -d libs &&
  rm "$USB_LIB_FILE"

env:
  # - BUILD_CONFIGURATION=Debug
  - BUILD_CONFIGURATION=Release

script: |
  mkdir build && cd build &&
  cmake .. -DCMAKE_BUILD_TYPE="$BUILD_CONFIGURATION" &&
  cmake --build .
